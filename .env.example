# StateSense 远端 LLM 供应器配置（shadow.provider = "http" 与
# wording.provider = "http" 共用同一组端点与密钥）。
#
# 用法：把本文件复制为 .env 并填入真实密钥。`.env` 已被 .gitignore 排除，
# 绝不入库；本示例文件只含非敏感信息（端点与模型名）。
#
# 查找顺序：先 CWD/.env，再配置文件同目录/.env，先找到的文件整体生效；
# 已存在的环境变量优先于文件内容。
#
# 发送面（阶段 3.2 的输入契约 / 文案上下文契约 v2）：影子每轮只发送六个
# 聚合统计量（ent/gray/work/total_active/window/late_night）；文案只发送
# WordingContext 白名单字段（统计量 + 状态 + 动作 + 回执上下文计数）。
# 两者都不含窗口标题、URL、应用名或任何屏幕文本——
# 白名单见 src/statesense/shadow/models.py 与 src/statesense/intervention/wording.py。

STATESENSE_LLM_URL=https://api.senseaudio.cn/v1/chat/completions
STATESENSE_LLM_MODEL=glm-5.3-flash
STATESENSE_LLM_API_KEY=sk-在这里填入你的密钥
