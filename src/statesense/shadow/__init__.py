"""影子模型信号（阶段 3.2 的输入契约 + 阶段 3.4 的候选信号）。

**这个包里的任何东西都不参与判定。** 候选只落库、只进只读报表；
`Decision`、闸门与投递看不到它 —— 因此「模型说了什么」永远不会改变
「今晚到底有没有弹窗」。要让模型影响投递，必须先按阶段 3.4 的要求
写出判定规格、并在真实样本上拿到稳定增益。

一句话概括这个包存在的理由：**先把「模型与规则哪里不一样」变成可查的数据，
再谈要不要让模型动手。**
"""
