# StateSense-Agent V0.5 验证日志

> 对应规格：`docs/specs/2026-09-16-v0.5-observability-and-replay-design.md`
> 对应计划：`docs/plans/2026-09-16-v0.5-implementation.md`
> 分支：`docs/v0.5-spec`，基于已合并的 `b595826`（PR #2）

---

## 1. 运行环境

| 组件 | 版本 / 状态 |
|---|---|
| OS | Windows 10 家庭中文版 Build 19045 |
| Python | 3.12.10 |
| uv | 0.11.6 |
| 运行期第三方依赖 | **0**（`dependencies = []` 未改动） |
| 测试 | pytest，**263 passed**（基线 154） |
| Screenpipe | **v0.4.50，已启动**，数据目录 `D:\Screenpipe`，`localhost:3030` 监听中 |
| `SCREENPIPE_LOCAL_API_KEY` | **已设置**（写入用户环境变量，`api_key_env` 指定的名字） |

> 本日志分两部分：§3–§4.2 是 Screenpipe **未运行**时用合成数据完成的验证；
> §4.4 起是 Screenpipe **真实运行**后的联调。第 7 节说明两者各覆盖到哪里。

---

## 2. 交付内容

`b595826..HEAD` 共 15 个提交，35 个文件，+5436 / −36。

| 提交 | 内容 |
|---|---|
| `01e9850` | V0.5 技术规格 |
| `3d5be4a` | 修正 CONTRIBUTING 过期事实 + 重基线化迭代路线 |
| `910249f` | V0.5 实施计划 |
| `408367a` | `ActivitySource` 契约 + `entries_minutes` |
| `c3a357a` | schema v4：`entries_minutes` 列 + `run_events` 表 |
| `933a65d` | 调度器两处运行事件落行 |
| `cfabc38` | `[report]` 配置节与校验 |
| `070bd3b` | `Store` 四个只读查询方法 |
| `11ac2c1` | `report` 聚合纯函数与结果模型 |
| `de8d9a2` | `report` 渲染 |
| `4389df4` | `--report` 接线 |
| `064e0a4` | `replay` 剧本/合成/驱动 |
| `899aaed` | 五个剧本与 `--replay` |
| `bd0d298` | 修复 `--replay` 不可重复 |
| `027bbfe` | 分类清单注释 |

---

## 3. 回放器：把「需要等」变成「不需要等」

```
$ uv run python -m statesense --replay all
PASS  ladder
PASS  outcome
PASS  gates
PASS  degraded
PASS  sleep_gap
WARN  high_risk_minutes=65 高于 window_minutes=60：ent 是窗口内条目分钟数之和，
      不可能超过窗口长度，因此 HIGH_RISK_PASSIVE_CONSUMPTION 不可达，
      其专属动作永远不会被选中。修正方向：把 high_risk_minutes 降到窗口以内，
      或加长 window_minutes
```

**这条输出把 V0 验证日志 §7 的前两条从「未验证」变成「已验证（机制层面）」：**

| V0 §7 原条目 | 现在的状态 |
|---|---|
| 真实长时段被动消费触发干预 | **机制已验证**：`ladder` 跑完 90 分钟虚拟时间，证明 20/40/65 阶梯真的产生状态迁移、且在 `PASSIVE_CONSUMPTION` 真的触发干预 |
| T+10min 行为回执实际落行 | **机制已验证**：`outcome` 用两个对照剧本证明 `outcomes` 表真的落行，且 `disengaged` / `continued` 两个标签都正确 |

`outcome` 剧本的两个对照：

- 娱乐在触发点**停止** → `before = 10` 分钟、`after = 0` 分钟 → `disengaged`
- 娱乐**继续** → `before = after = 10` 分钟 → `continued`

`gates` 剧本覆盖了 8 次 `daily_cap` 触顶与 cooldown 边界（300 分钟虚拟时间、61 轮）：

- 触发次数恰好等于 `daily_cap = 8`，一次不多
- 存在被 `cooldown` 以「不足阈值」挡下的轮次（`value < 30`）
- 任意两次干预间隔都 ≥ 30 分钟
- 存在以 `daily_cap` 为首个失败闸门的轮次

`degraded` 剧本守住核心安全属性：`data_status = unreachable` 时逐轮落行、`skipped = 1`、
**没有任何干预发生**。这正是 V0 审查发现的最严重问题之一（非 ok 数据下仍写 `state=NORMAL`
且无法分辨）的回归锁。

`sleep_gap` 剧本证明健康信号成立：200 分钟空档不落 `evaluations` 行，但落一条
`run_events(kind='sleep_gap', detail='200.0')`。

---

## 4. 观测层

### 4.1 空库

```
$ uv run python -m statesense --report --config config/config.toml --db <空库>
概览
  区间内无任何评估记录。

结论：进程从未跑起来，或库路径不对。
exit=0
```

空库不是错误 —— 「进程从未跑起来」本身就是观测结论。

### 4.2 种入数据的完整视图

用手工种入的 9 轮评估（含一次被 `ratio_min` 挡下、一次真触发、一轮取不到数据、
一个 Brotato 式漏判锚点、一处 200 分钟缺口）跑 `--views all`，六个视图全部有输出：

```
判定面
  评估轮数    9
  状态分布    NORMAL=7  PASSIVE_CONSUMPTION=2
  data_status ok=8  unreachable=1
  skipped     1（这些轮的 state 不代表「真的正常」）

闸门面
  各闸门阻挡  state_min=6  ratio_min=1
  占比分布    0.0-0.1=5  0.1-0.2=1  0.7-0.8=1  0.8-0.9=1
  该提醒未提醒 1 轮（state_min 通过却被后续闸门挡下）
    2026-09-16 20:20:00+08:00  ratio_min  value=0.7 阈值=0.75

干预面
  触发次数    1
  用户回执    accepted=1
  被 cooldown 挡 0 轮   被 daily_cap 挡 0 轮

效果面
  回执分布    disengaged=1
  干预前娱乐  48.0
  干预后娱乐  4.0
  按用户回执分层（V0 规格 §16 风险 6 要求的缓解措施）：
    accepted   disengaged=1

疑似漏判（这一轮有活动，但规则一条都没命中）
  2026-09-16 20:35:00+08:00  活跃=59.9  未归类=59.9（100.0%）  明细缺失=0.0
```

**效果面的分层是 V0 规格 §16 风险 6 写明要做、而 V0 里没有任何地方能执行的那件事。** 现在它落地了。

概览视图同时给出了缺口及其成因：

```
  缺口        1 处
    2026-09-16 20:35:00+08:00 → 2026-09-16 23:55:00+08:00   200.0 分钟
      有运行事件记录：sleep_gap(200.0)
```

同一处缺口若**没有**运行事件记录，输出会写成「无运行事件记录 → 进程当时不在运行」——
这正是本版本要消灭的那个二义性的正面表述。

### 4.3 schema 守卫在真实旧库上生效

本机 `config/statesense.db` 是 V0 测试遗留的 **v3 旧库**：

```
$ uv run python -m statesense --report --config config/config.toml
库 schema 为 v3，需要 v4；请先跑一次 --once 完成迁移（--report 不做任何写入）
exit=2
```

`--report` 拒绝读取并拒绝迁移 —— 它承诺只读，就不该顺手改库。

### 4.4 真实 Screenpipe 联调

启动 recorder 并设置 `SCREENPIPE_LOCAL_API_KEY` 之后，逐条跑真实路径。

**① `--check`**

```
配置        OK（store=D:\AI project\StateSense-Agent\config\statesense.db）
通道        foreground_popup
ratio_min   0.75
回看窗口    最近 60 分钟
data_status ok
取到 1 条窗口记录，总活跃 0.2 分钟
     0.2 分钟  获取https://github.com/Bairea/StateSen — DSH 本地构建
exit=0
```

**② v3 → v4 迁移在真实旧库上完成**

生产库是 V0 测试遗留的 v3。跑一次 `--once`：

```
迁移前 user_version = 3
迁移后 user_version = 4
表: evaluations, interventions, outcomes, kv, run_events
evaluations 列: [... 'skipped', 'entries_minutes']
```

旧行的 `entries_minutes` 取 `DEFAULT 0` —— 不伪造数据。库中 5 行里，2 行是旧行
（`entries_minutes=0`）、3 行是新写的。

**③ `--once --dry-run` 真实写库**

```
state=NORMAL intervened=False outcomes_closed=0 note=state_min 未通过（0.0 vs 阈值 1.0）；ratio_min 未通过（0.0 vs 阈值 0.75）
```

**④ `--report` 的缺口视图在真实场景下解掉二义**

```
缺口        2 处
  2026-09-16 19:19:09 → 2026-09-16 20:13:12   54.0 分钟
    无运行事件记录 → 进程当时不在运行
  2026-09-16 20:13:12 → 2026-09-17 12:34:08   980.9 分钟
    无运行事件记录 → 进程当时不在运行
```

这是健康信号第一次在真实数据上生效：`run_events` 是 0 行，而进程**确实**没在跑
（昨天停的、今天才启动）。没有这个视图，这 980 分钟与"崩在日志里"是分不清的。

**⑤ 探测「`ent` 能不能超过窗口长度」**（决定 §6.1 那个结构性发现）

对最近 72 小时逐小时取 60 分钟窗口：

```
data_status 分布: {'empty_but_recording': 64, 'ok': 7}
sum(entries) 最大值: 60.10   （窗口长度 60）
超过窗口长度的窗口数: 1
```

**结论：能超过，但只超 0.1 分钟（0.17%）。** 要到 `high_risk_minutes=65` 需要 108% 的
重复计数。所以 §6.1 的结论不变，只是理由从"数学上不可能"改成"需要 8% 的重复计数，
实测只有 0.17%"。

同时**没有发现多显示器造成的成倍重复** —— 见下一条。

**⑥ 真实分类明细**

```
最新一轮: total=3.2  ent=0.0  gray=0.0  work=1.9  entries=3.2
  1.90 分钟  [WORK]   electron.exe  '获取https://github.com/Bairea/StateSen — DSH 本地构建'
  0.90 分钟  [OTHER]  electron.exe  '阅读能构筑…— DSH 本地构建'
  0.40 分钟  [OTHER]  electron.exe  '测试 spec02 构筑内容 — DSH 本地构建'
```

三处都对：`entries_minutes = 1.9+0.9+0.4 = 3.2` **精确等于** `total_active_minutes`
（没有重复计数）；`work=1.9` 命中 `github` 规则；那 1.3 分钟 OTHER 正是"我在与 agent
对话的窗口标题"，规则不命中它 —— 漏判视图的语义正确。

**⑦ `--from-screenpipe` 真实回查（此前标注为「未验证」）**

把临时配置的 `leak_min_unclassified_ratio` 压到 0.2 让锚点触发（正常阈值 0.7 下不该触发
这么低的活动量）：

```
疑似漏判（这一轮有活动，但规则一条都没命中）
  2026-09-17 12:34:08  活跃=3.2  未归类=1.3（40.6%）  明细缺失=0.0
  窗口明细（仅打印，不落库）：
    2026-09-17 12:34:08    0.9 分钟  阅读能构筑…— DSH 本地构建
    2026-09-17 12:34:08    0.4 分钟  测试 spec02 构筑内容 — DSH 本地构建
```

回查真实 Screenpipe 拿到了明细，**标题只打印不落库** —— §5.3 的数据最小化守住。

**⑧ `--trace` 与 `--daemon`**

`--trace` 在真实数据上输出了逐轮闸门明细。**它的输出暴露了缺陷 8**（下节）。

`--daemon` 启动后立即评估了一轮（新行落在 12:39:41），随后按 60 秒 tick 继续 ——
常驻路径首次实测可运行。

---

## 5. 实施中发现并修正的缺陷

| # | 缺陷 | 怎么发现的 | 修法 |
|---|---|---|---|
| 1 | `ScriptedReader` 用 `captured_at` 当窗口末端，而真实 `ActivityReader.read(start, end, ...)` 用 `start`/`end` | `outcome` 剧本的 `stopped` 分支算出 `ent_before=0.0`（应为 10.0）→ 回执永远是 `no_data` | `snapshot_at` 改为按 `start`/`end` 裁剪，并补一条专门的测试 |
| 2 | `--replay` **不可重复**：上一轮的库文件还在，行数累加 | 端到端跑**第二遍**时 `degraded` 从 6 行变 12 行、`sleep_gap` 从 1 行变 2 行。pytest 里没暴露，因为 `tmp_path` 每次是新的 | `check_scenario` 每次重建工作目录，并补两条重复性测试 |
| 3 | 计划里的 `run_events` 测试用同一时刻插两条 | 写测试时发现 `at` 是主键且用 `INSERT OR REPLACE`，第二条会覆盖第一条 | 测试改用不同时间戳，另加一条「同刻幂等」测试 |
| 4 | `test_report_config` 的 helper 向已含 `[report]` 的示例追加同名表 | TOML 报重复表，9 个测试全红 | helper 先摘掉示例里的 `[report]` 节；另加「示例值必须等于代码默认值」的漂移测试 |
| 5 | `test_gate_breakdown_survives_corrupt_trace` 断言「坏数据仍计数」 | 语义写反了 —— 坏行应当跳过、其余照常 | 改成两行数据、坏一行、断言好行仍被统计 |
| 6 | 配置带 UTF-8 BOM 时 `tomllib` 报 `Invalid statement (at line 1, column 1)` | **真实联调**：用 PowerShell 生成临时配置时带上 BOM，位置指向文件开头，用户完全看不出原因。Windows 记事本默认就写 BOM | `load_config` 显式剥掉 BOM，并补两条测试 |
| 7 | TOML 语法错误**没有被包成 `ConfigError`**，`main` 接不住 | **真实联调**：用户看到的是 Python traceback 加退出码 1，而不是「配置错误：…」加退出码 2 | 解析失败包成 `ConfigError`；补一条断言「无 Traceback」的 CLI 测试 |
| 8 | `--format json` 会输出裸 `Infinity`，不是合法 JSON | **真实联调**：V0 时期写的旧行 `gate_trace` 里存着 `Infinity`（表示「从未干预过」），`report` 如实读出并原样序列化。规格 §5.4 明说 JSON 是给 `jq` 用的，而 `jq` 会直接拒绝 | `render_json` 把非有限浮点转 `null`（正好对应 `value=None` 的既有语义），并用 `allow_nan=False` 兜底；补两条严格解析测试 |

**缺陷 1 与 2 都是"回放器验证回放器"发现的。** 缺陷 2 尤其说明问题：一个不可重复的
验证工具比没有验证工具更糟 —— 它会在第二次运行时给出假结果。这条只会在真实重复执行时暴露，
纯单元测试永远抓不到。

**缺陷 6、7、8 只有真实跑起来才会暴露。** 前六个都在 `pytest` 全绿的情况下存在 ——
它们全部落在"测试覆盖不到的接缝"上：真实编码环境、真实错误路径、旧版本遗留的数据。

缺陷 8 尤其值得记一笔：它**不是本次引入的**，是 V0 时期写进库的旧数据暴露了 report 的
一个健壮性缺口。`report` 读的是历史行，包括旧版本写的行 —— 这一条以后还会再遇到。

---

## 6. 结构性发现（未修，交 owner 决定）

### 6.1 `HIGH_RISK_PASSIVE_CONSUMPTION` 在默认配置下不可达

`ent_minutes` 是窗口内条目分钟数之和，**不可能超过窗口长度**。而默认配置：

```
window_minutes     = 60
high_risk_minutes  = 65
```

于是 `ent >= 65` 永远不成立。后果：

- `HIGH_RISK_PASSIVE_CONSUMPTION` 是**死状态**
- 动作池里 `applies_to = ["HIGH_RISK_PASSIVE_CONSUMPTION"]` 的 `reading`（读一段《道德经》）**永远不会被选中**

`ladder` 剧本的 90 分钟连续娱乐实测 `ent` 上限为 60，从未触到 65。

**真实数据把这个前提测了一半。** §4.4 第 ⑤ 项的探测显示：单个 60 分钟窗口内
`sum(entries)` 最大达到 **60.10** —— 确实能超过窗口长度，但只超 0.17%。
要到 65 需要 108% 的重复计数。所以结论不变，只是理由要改写：

> 不是"数学上不可能"，而是"需要 8% 的重复计数，实测只有 0.17%"。

同时该探测也**没有发现多显示器造成的成倍重复**（§4.4 第 ⑥ 项：`entries_minutes`
精确等于 `total_active_minutes`）。

这不是实现缺陷，是 V0 继承自 `ref1.md` 的 20/40/65 刻度与 60 分钟窗口不一致。
两个修法各有代价：

| 修法 | 代价 |
|---|---|
| `high_risk_minutes` 降到窗口以内（如 55） | 偏离 `ref1.md` 的 65 刻度 |
| `window_minutes` 加长到 70+ | 改变分析窗口；V0 规格里「60 分钟窗口内 `ent>=40` 隐含 `ratio>=0.667`」的推导需重算 |

**本次不改，只让它在每次 `--replay` 时以 WARN 现形。** `structural_findings()` 会持续提示，
直到配置改对为止 —— 有测试锁住这个行为。

### 6.2 漏判锚点的 0.7 阈值在清单薄弱时会偏噪

种入数据时，9 轮里 6 轮触发了漏判锚点 —— 因为那些轮次只有 5 分钟娱乐、其余 55 分钟
是"未命中任何规则"的活动。这在语义上**是对的**（60 分钟没被任何规则解释确实值得看），
但它说明锚点的信噪比直接取决于分类清单配得有多全。

这与 §8 的分类清单个性化是同一件事的两面：清单越全，锚点越准。
规格已写明 `0.7 是起点，用真实分布再调`。

---

## 7. 尚未验证的部分（诚实清单）

| 项 | 状态 | 原因 |
|---|---|---|
| `--check` / `--once` / `--report` / `--trace` / `--daemon` 真实路径 | **已验证** | §4.4 |
| v3 → v4 迁移在真实旧库上 | **已验证** | §4.4 第 ② 项 |
| 二级漏判视图（`--from-screenpipe`）真实回查 | **已验证** | §4.4 第 ⑦ 项 |
| 缺口视图在真实场景下解掉二义 | **已验证** | §4.4 第 ④ 项：980 分钟缺口正确归因为"进程没在跑" |
| 真实长时段被动消费触发干预 | **仍属机制层** | 回放证明的是「阈值到达时会触发」，不是「真实使用时时机对不对」。合成数据是人编的 |
| 分类清单在真实使用下的准确率 | **未验证** | 只能靠 `--report --views 5` 在多天真实数据上迭代 |
| 挂常驻跑 5 天 + 缺口视图与实际睡眠对账 | **未验证** | `--daemon` 已实测能启动并评估，但**未连续跑过多天**；任务计划程序也尚未注册 |
| `ratio_min` 基于真实分布的校准 | **未完成** | 判据 4 要求「有依据」，而依据来自 5 天真实数据。当前库只有几轮、且娱乐分钟数近零 |
| 全屏下由**调度器**触发的弹窗 | **V0 已间接验证** | 未改动投递通道 |
| 真实触发一次干预（弹窗 + 按钮回执 + T+10min 回执落行） | **未验证** | 需要真实刷够 40 分钟娱乐。回放验的是机制，不是真实链路 |

> 真实联调期间的 `--check` 返回 2 是**启动 recorder 之前**的状态：

```
配置错误：环境变量 SCREENPIPE_LOCAL_API_KEY 未设置；可用 `screenpipe auth token` 获取后写入该变量
```

**这是预期的**（当时 Screenpipe 未运行 + key 未设），不是缺陷。启动 recorder 并设置 key
之后，§4.4 已把涉及真实 Screenpipe 的路径逐条跑过。

---

## 8. 与规格的偏差

| 规格条目 | 实现情况 | 理由 |
|---|---|---|
| §11 CLI 的 `--replay --keep-db` | **未实现** | 剧本的库**始终保留**在 `replay/<剧本>/` 下，因此该 flag 没有意义。`--report --db replay/ladder/replay-ladder.db` 照常可读 —— 规格 §6.7 的「回放产出数据、report 消费数据」回路仍然成立 |
| §7.2 `run_events.kind` 封闭枚举 | 已实现，且写入未知类型抛 `ValueError` | — |
| §9 `entries_minutes` **无**默认值 | 已实现 | 写计划时收紧：`0.0` 本身就是可疑信号，静默默认会让漏判检测失效。规格 §9 与 §15 已同步修正 |

---

## 9. 下一步

1. **决定 `high_risk_minutes` 与 `window_minutes` 的关系**（§6.1）—— 这是 20/40/65 刻度
   第一次与窗口长度对齐，需要一个明确取舍。真实数据已把前提测掉一半（§4.4 第 ⑤ 项）。
2. ~~补上真实 Screenpipe 路径的验证~~ —— **已完成**，见 §4.4。
3. **挂常驻**：`--daemon` 已实测能启动并评估（§4.4 第 ⑧ 项）。注册任务计划程序，
   勾选「失败后重启」。
4. **观察 5 天**，用 `--report --views 2` 的真实 `ent_ratio` 分布决定 `ratio_min` 调或不调；
   用 `--views 1` 的缺口列表与实际睡眠/重启记录对账，验证健康信号。
5. 依据 `--views 4` 的分层结果，检查「离开电脑被误算成 `disengaged`」的比例
   （V0 规格 §16 风险 6）。
6. **等一次真实触发**。当前库里的娱乐分钟数近零，`interventions` 一行都没有 ——
   弹窗、按钮回执、T+10min 回执落行这三段真实链路**一次都没跑过**。
   这是本版剩下最大的一块未验证。

### 本机当前状态

| 项 | 状态 |
|---|---|
| Screenpipe recorder | **运行中**（`pwsh-1`，数据目录 `D:\Screenpipe`） |
| statesense daemon | **已停止**（验证用，未注册任务计划程序） |
| `SCREENPIPE_LOCAL_API_KEY` | 已写入**用户环境变量**（`api_key_env` 指定的名字） |
| 生产库 | `config/statesense.db`，schema v4，5 行评估、0 行干预 |

重启 recorder：

```powershell
screenpipe record --data-dir "D:\Screenpipe" --disable-audio --retention-days 14
```
